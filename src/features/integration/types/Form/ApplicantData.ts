export default interface IApplicantData {
    name: string,
    address: string;
    postalCode: string;
    city: string;
    phoneNumber: string;
    email: string;
    accessCode: string;
    paragraph: string;
}