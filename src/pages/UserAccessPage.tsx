import UserAccess from '../features/userAccess/UserAccess';
import { RouteComponent } from '../routes/Route';

const UserAccessPage: RouteComponent = (props) => <UserAccess {...props} />;

export default UserAccessPage;
