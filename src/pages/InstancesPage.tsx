import Instances from '../features/instances/Instances';
import { RouteComponent } from '../routes/Route';

const InstancesPage: RouteComponent = (props) => <Instances {...props} />;

export default InstancesPage;
