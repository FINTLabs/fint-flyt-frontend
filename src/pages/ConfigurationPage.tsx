import Configuration from '../features/configuration/Configuration';
import { RouteComponent } from '../routes/Route';

const ConfigurationPage: RouteComponent = (props) => <Configuration {...props} />;

export default ConfigurationPage;
